{
  "name": "whatchagot",
  "version": "1.0.0",
  "description": "Recipe and Ingredient management app for users to keep track of the things in their kitchen and find recipes to meet their needs.",
  "main": "index.js",
  "scripts": {
    "format": "npx prettier . --write",
    "lint": "npx eslint .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd frontend && npm start && cd ../backend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "create-react-app": "^5.0.1",
    "eslint-config-standard": "^17.1.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
